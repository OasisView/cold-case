// Landing page — bullseye background + hero content + "Enter" CTA
// TODO: build full landing page (Phase 3+)
import { redirect } from "next/navigation";

export default function LandingPage() {
  redirect("/dashboard");
}
